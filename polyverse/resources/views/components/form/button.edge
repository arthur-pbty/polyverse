<button 
  id="{{ name }}" 
  name="{{ name }}" 
  type="{{ type ? type : 'submit' }}" 
  class="bg-vert text-white font-bold py-2 px-4 rounded focus:outline-none focus:ring-2 focus:ring-vert focus:ring-opacity-50">
  {{ text }}
</button>